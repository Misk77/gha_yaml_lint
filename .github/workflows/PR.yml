---
name: PR
on:
  workflow_run:
    workflows: ["syntax validation"]
  pull_request:
    branches: [main]

jobs:
  PR-build:
    name: 
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
